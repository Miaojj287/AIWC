# AIWC 布局逻辑与行为规格

> **分工**：Figma 是视觉真源（尺寸、颜色、文案、每个状态长什么样），本文是**逻辑真源**（区域怎么组合、什么驱动什么、状态怎么流转、哪些是动态数据）。两者冲突时，视觉听 Figma，行为听本文。
> Figma：`https://www.figma.com/design/l1f9ojImxG3cosmDbtUZT2/AIWC`（Page 3）。节点索引见 §9。
> 设计原则（不可漂移的约束）见 `CLAUDE.md`。
> **稿子里所有具体内容都是演示数据**：联系人、群名、消息、wxid、密钥、路径、模型名、统计数字、时间。实现时全部来自真实数据或配置，不要写死。

---

## 0. 全局布局逻辑

### 0.1 四列容器
```
Window
├─ WindowChrome   固定高 48（macOS 红黄绿灯 + 居中标题）
└─ Body           剩余高度，横向四列
   ├─ IconRail        固定宽 64
   ├─ ObjectList      固定宽 280（内容由 rail 选中的功能决定）
   ├─ Workspace       fill（Tab 容器）
   └─ AgentPanel      固定宽 360
```
- 设计稿按 1440×900 画；实际窗口可拉伸，**只有 Workspace 吸收宽度变化，只有 Body 吸收高度变化**。最小窗口宽建议 1200（此时 Workspace ≈ 496）。
- 三个固定列内部各自滚动；Workspace 内每个 Tab 自己管理滚动。Chrome、Tab 栏、各列的 header 与 footer 都是 sticky。
- 三层背景（壳 / 面板 / 内容）的分配规则：rail 和 Tab 栏用壳色；两侧栏用面板色；Workspace 内容用内容色；激活 Tab 与它下方内容同色以形成「连成一片」的效果（Agent 面板的激活 Tab 同理，用面板色）。

### 0.2 功能 → 左列 → 默认 Tab
rail 上的三个功能各有一套「对象列表 + 工作区内容」：

| rail 选中 | ObjectList 显示 | 点击列表项在 Workspace 打开的 Tab |
|---|---|---|
| 聊天 | 会话列表（单聊 + 群聊） | 该会话的聊天预览 |
| 自动回复 | 会话列表（带规则状态与开关） | 该会话的自动回复规则编辑页 |
| AI 克隆 | 联系人列表（仅单聊，带克隆状态） | 该联系人的克隆页（状态决定显示确认页 / 进度 / 已克隆页） |

- 切换 rail 只替换左列并激活对应功能的「最近一个 Tab」（没有则显示该功能的空态）；**不关闭其他 Tab**。
- rail 底部头像 → 打开「设置」Tab（全局唯一，已存在则激活）。设置打开时头像加描边，rail 上的功能选中态保持不变。

### 0.3 Tab 容器规则
- 每个对象在 Workspace 里**唯一**：同一会话再次点击只激活已有 Tab；不同功能对同一会话打开的是不同 Tab（「产品市场群」与「自动回复 · 产品市场群」并存）。
- Tab 类型：聊天预览、自动回复规则、克隆页、设置、文件（Agent 生成的 Markdown 等）。每类有自己的图标；文件 Tab 名 = 文件名。
- Tab 可关闭（×）、可拖拽排序、可固定（固定的排最左且不显示 ×）、右键有关闭其他 / 关闭右侧 / 恢复已关闭。
- 关闭带未保存修改的 Tab（自动回复规则、记忆编辑器）先弹「放弃修改？」。
- Tab 过多时横向滚动，右端 `···` 提供标签管理菜单。
- Agent 面板不属于 Tab 容器，切 Tab 不影响它。

---

## 1. 聊天功能（Main）

### 1.1 会话列表
- 数据：本地索引出的全部会话；每项 = 头像（单聊首字 / 群九宫格）、名称、最近消息时间、最近一条消息预览（群聊带发送者前缀）、未读数。
- 排序：置顶 → 最近消息时间倒序。
- 顶部筛选 chip：全部 / 单聊 / 群聊，chip 上带实时计数；筛选图标是「更多筛选」下拉（仅未读 / 已静音 / 有附件…），多个筛选可叠加，有筛选时 chip 区显示「清除筛选」。
- 搜索：focus 即出实时结果（匹配会话名 + 命中消息数），回车进入全文搜索（在当前会话 Tab 内高亮结果）。
- 选中项 = 当前激活 Tab 对应的会话；关闭 Tab 不改变列表选中，切 Tab 会同步选中。
- 未读徽标：会话有未读时显示数字，打开该会话后清零；静音会话只显示小点。
- hover 露出 `···`（= 右键菜单）。右键菜单动作映射：置顶 / 已读 / 静音 → 本地状态；设置自动回复 / 克隆 → 打开对应功能的 Tab 并切换 rail；引用到 Agent → 把该会话作为 @ 引用塞进 Agent 输入框；从列表隐藏 → 仅隐藏，可在筛选里恢复。
- 刷新按钮 = 触发一次增量同步，同步中按钮转圈、tooltip 显示上次同步时间。
- 加载失败时按连接状态给空态，不看错误文字：未配置 / 未解锁 / 连接出错 →「连接微信」引导；连接中或状态未返回 → 骨架；已连接 → 可重试的错误态。

### 1.2 聊天预览 Tab
从上到下四段，其中只有消息流滚动：
1. **头部**：对象头像 + 名称 + 元信息（类型 · 人数 · 索引到的时间）+ `···`（会话级菜单：信息 / 搜索 / 导出 / 设自动回复 / 克隆 / 重建索引 / 移除索引）。
2. **同步条**：状态机 `已同步(上次时间 + 统计) → 同步中(进度) → 失败(错误 + 重试)`；右侧两个下拉是**当前 Tab 的过滤条件**：日期范围（预设 + 自定义）、发送者（多选）。过滤只影响消息流与导出范围，不影响其他 Tab。
3. **消息流**：按时间正序，**底对齐、初始滚到最新**，向上滚动分页加载更早消息。跨天插日期胶囊。消息类型至少支持：文本、文件、语音（可播放 + 转文字展开）、图片、系统消息；我方 / 他方由发送者 wxid 是否等于当前账号判定，我方靠右用深蓝气泡，他方靠左带名字。hover 出悬浮工具条，右键出菜单，勾选进入多选模式。
4. **底部操作条**：全选 / 下载（导出弹窗，范围默认 = 当前过滤结果，勾选后变为已勾选）/ 删除（只删本地缓存与索引，二次确认）/ 引用到 Agent（把当前会话或已勾选消息送进 Agent 上下文）。
- 多选模式：顶部出现选择条（计数 + 批量动作 + 取消），退出方式：取消或清空勾选。

### 1.3 Agent 面板
- **多会话**：顶部 Tab 条，一个会话一个 Tab；`+` 新建，🕘 历史下拉（最近会话 + 搜索 + 查看全部），`···` 会话菜单（重命名 / 导出 / 压缩上下文 / 清空 / 删除）。新会话默认关联当前激活的 Workspace Tab 作为上下文引用。
- **消息模型**（从上到下渲染）：用户消息（带引用 chip）→ Agent 文本 → 工具调用组（每次调用一行：状态 ✓/⟳/○/!、一句话、耗时；可展开输入 / 输出）→ 结果卡（生成的文件，点击在 Workspace 开 Tab）→ 文本 → 建议操作按钮。等待确认的工具调用在行内显示「等待确认」，并在该行**正下方内联**展开确认卡（不是浮层，随消息流滚动）。
- **权限模式**：Ask（默认；读取直接执行，写入本机数据前询问）/ Bypass（读写都直接执行，对外发送仍问）/ Autopilot（连对外发送——微信消息、推送表格——也直接执行）。**破坏性操作在任何模式下都必须二次确认**，普通工具调用不再逐次打断。下拉里每个模式带一句说明，`Ask / Bypass / Autopilot` 名称不翻译。模式是会话级设置，在输入框工具栏切换；确认卡里「总是允许」把该工具加入白名单（在设置 › 权限规则里可撤销），破坏性操作没有这个选项。
- **引用**：输入 `@` 弹 popover，四类：会话 / 文件（Workspace 打开的文件）/ 联系人 / 记忆文件；选中后变 chip，chip 可删。输入 `/` 弹技能列表。
- **证据引用**（Agent 回答里的出处）：模型把工具结果里的 `cite` 原样粘贴成 `[02-14 19:28](wx://会话/消息)`，面板渲染为行内 chip（h18、r100、微文字 11、徽标配方：色 14% 底 + 30% 描边 + 100% 字）。chip 显示「发送者 · 时间」，挂载后逐字核对它前面引号里的话：原文一致或无引号 = 强调色；引号里的话不在这条消息里 = 警告色 + ⚠；消息在本地不存在 = 危险色。hover tooltip 显示原文摘录；点击 = `tab.openChat` 定位：该会话已开 Tab 则激活并滚动到那条消息（高亮 1.8s），没开则新开 Tab；只知道消息 id 时先查出真实位置再加载前后窗口，当前 Tab 的筛选会藏住目标时先清空筛选并 Toast 说明；找不到时 Toast「没有找到这条消息」。回答结束时主进程也会逐字核对一遍，发现对不上会让 Agent 紧接着补一段「更正」（每轮最多一次）。
- **上下文计量**：工具栏常驻环形进度 = 已用 / 模型上限；hover 显示明细（系统提示 + 记忆 / 引用消息 / 对话历史）；超过阈值（建议 80%）自动提示压缩；压缩会把历史整理成摘要，插入「上下文已压缩」提示条。
- **流式**：每轮只在消息流末尾显示一个运行状态；等待审批时改为等待确认。停止入口只保留输入框发送位置的停止按钮，消息区不重复放停止按钮或逐段等待动画。空的模型占位不占空间。失败显示错误卡并给可执行按钮（去改 Key / 重试）。
- **消息操作**：一轮内的中间说明、工具调用及更正连续展示；只在已结束轮次的最后一段非空回复下常显一组图标操作（复制 / 重新生成 / 反馈）与模型、耗时，图标带 tooltip 和无障碍名称。生成中不提供重新生成或重新发送。用户消息的编辑 / 复制 / 重发常显在正文下方，不覆盖文字；工具详情展开箭头常显并支持键盘操作。hover 不再影响消息操作栏的可见性与布局。
- **模型**：工具栏下拉列出已配置模型（设置 › AI 接入）+ 本地模型徽标；切换只影响当前会话。
- 空会话显示建议提示（基于当前引用的会话生成）。

---

## 2. 设置 Tab

- 布局：左 200 子导航（账号块 + 搜索 + 分组菜单），右内容区滚动；导航项 = 一个内容页。搜索设置直接列出匹配的设置项（带所属页 / 分组），点击跳到该页并高亮该行。
- 内容页统一由「Section 标题 + 卡片 + 行」组成；行 = 标题 + 一句说明 + 右侧控件；需要整行输入的字段用 stacked 行。
- **生效时机**：开关 / 分段 / 下拉 → 即时生效并本地持久化，不需要保存按钮；需要校验的多字段（模型接入、记忆文件）→ 显式「测试连接 / 保存」。账号密钥直接在原输入框编辑，回车或失焦时校验保存，Escape 撤销；默认打码，保留显示与复制，错误在输入框下提示。新接入的 AI 模型默认关闭，用户手动启用后才进入默认模型候选；同步保留已有启用状态。
- 各页逻辑：
  - **常规**：主题（浅 / 深，即时）；语言（分段控件 `简体中文 | English`，语言名始终用各自原文；即时生效并持久化到 `general.language`，界面、原生菜单、Toast、主进程报错与导出文件表头同步切换；首次启动跟随系统语言）；启动（开机自启；关闭窗口行为 = 退出 / 最小化到菜单栏 / 每次询问，「每次询问」时关闭窗口弹选择框并可勾选记住）。
  - **账号**：顶部当前账号卡（切换账号下拉列出本机检测到的所有 wxid 及验证状态；重新扫描重新枚举目录）。数据库配置 = 解密密钥（自动获取：kvcomm + wxid 验真优先，失败回退内存扫描，全程进度弹窗；也可手动粘贴，校验 64 位 hex）+ 数据库根目录 + 账号验证（候选 wxid 下拉 → 验证目录与密钥匹配 → 测试连接给出成功 / 失败 Toast 与行内错误）。缓存目录可改可恢复默认，磁盘空间不足给警告。图片密钥（XOR / AES）同样自动获取，缺失时提示回退内存扫描。
  - **AI 接入**（Figma `164:3`「用户配置大模型」，稿子里的「配置自定义模型」弹窗在 AIWC 里就是这一页的卡片）：Agent 模型 = 一张两栏卡：左「模型厂商」栏（DeepSeek / Kimi / GLM / Qwen / MiniMax / Hunyuan / Seed / OpenAI / Anthropic / Gemini / Ollama 预设 + 已配置的自定义接口 + 「+ 自定义」；状态点绿 = 已接入、红 = 上次测试失败；卡片窄于 560 时栏变成顶部横向条），右侧 = 当前厂商面板：头部（图标 + 名称 + 平台名 / 连接状态 + 「测试连接」；未接入 → 主按钮「接入」，已接入 → 「编辑 Key」「断开」，自定义接口多一个「编辑」）+ 模型行（名称 + 推理强度 / 快速 徽标 + 一句说明 · 上下文 ｜ 「启用」开关 ｜ ✎ → 浮层：推理强度 默认 / 关闭 / 低 / 中 / 高 / 极高、快速开关、上下文用量、移除此模型）+ 「添加模型」（输入 ID 或从接口拉取）。未接入时预设模型照常列出但开关禁用。「接入」弹窗 = 厂商行 + 接口地址 + API Key（「前往官网获取」）+ 可用模型 chip（预设 / 拉取 / 手输，多选）+ 底部左「连接测试」右 取消 / 确认；自定义接口多 名称 + 兼容方式。断开 = 危险确认并删除钥匙串 Key。默认模型行不变。语音转文字 = 本地 / 在线两个 Tab：本地列出模型及下载状态（下载中显示进度可取消；删除需确认；可设默认），在线表单沿用旧的行式模型表单（`ModelForm`）。
  - **记忆**：四个 Markdown 文件（MEMORY / USER / SOUL / AGENTS）列表 + 内嵌编辑器；编辑器有脏状态（「已修改 · 未保存」）、保存 / 重置；策略 = 自动写入、写入前确认、条数上限、清空（输入确认词）。Agent 写入记忆时右下 Toast 通知。
  - **版本与支持**：版本信息 + 检查更新（loading → 已是最新 / 发现新版本弹窗 → 下载进度）+ 协议 / 隐私（弹窗内滚动）+ 导出日志（完成后 Toast 可定位文件）。

---

## 3. 自动回复

- **规则模型**：每个会话至多一条规则 = { 启用, 发送方式(自动回复 / 全自动回复), 回复方式(固定文案 / AI 生成), 固定文案(支持变量 {昵称}{时间}{群名}), System Prompt, 参考历史条数 }。**只有这些**——凡是「开着却不回」的隐藏闸门（触发条件、生效时段、全局工作模式、全局总开关）都已删除；「发送方式」是编辑页和列表第二行上都看得见的设置，不是隐藏闸门。会话开关就是唯一的开关：开着 = 收到消息就写回复。
- **发送方式**（编辑页第一行，分段控件）：
  - **自动回复**（新规则默认）：回复写好后停在「最近自动回复」那条记录里，记录展开成可编辑草稿 + 忽略 / 确认发送（改过文字后变为「修改后发送」）；点确认才发出。同时右下 Toast「回复已写好，等你确认后发送」带「去确认」，回复台里也有同一条。等待中的草稿不会自己过期；对方又发来新消息时旧草稿作废、按最新那条重新写，所以按钮永远对应最新未回的消息。重启应用后草稿还在，不会为同一条消息重复生成。
  - **全自动回复**：写好后倒计时（默认 5s）自动发出，倒计时期间记录上有「取消发送」。升级前已存在的规则保持全自动。
- **左列**：会话列表复用 §1.1 的条目，但第二行显示规则状态（确认后发送 / 自动发送 · 回复方式 · 参考 N 条 · 今日次数 / 已暂停 · 原因 / 未设置），有待确认的回复时状态点换成「待确认 N」徽标并排到最前；右侧开关直接启停规则；顶部分段 = 全部 / 已开启 / 已暂停。会话从后端分页加载、在后端搜索（与聊天列表一致）；有规则的会话单独补齐，始终完整列出并排在最前，不受分页影响。复制规则弹窗同样分页、在后端搜索。
- **编辑页**：头部（会话 + 状态徽标 + 启用开关 + `···`）；两张卡分别是回复方式、最近记录；底部保存条显示上次保存时间与脏状态。微信未连接 / 演示数据 / 发送已熔断时，顶部才多出一张状态卡（熔断附「我已确认，恢复自动发送」按钮），一切正常时不占位置。
  - 回复方式切换整块替换后续行：固定文案（文案 + 变量 chip）/ AI 生成（System Prompt + 参考历史条数）。
  - 固定文案为空、参考历史条数越界 → 保存禁用并行内报错。
  - 保存 → Toast「立即生效」；取消 / 关 Tab 有未保存修改 → 确认弹窗。
- **开启即补回**：打开会话开关时，若该会话最后一条是对方发的（也就是还没人回），立刻按规则回一次，不用等下一条新消息。自然幂等——回过之后最后一条就是我们自己的，重启 / 重连 / 再开都不会重复回。
- **失败不算已回**：一次尝试没能把消息发出去（模型报错、余额不足、发送校验失败、被更新的消息取代），这条消息重新变回「待回复」，下一次开启 / 重启 / 手动触发还能再试。只有真正发出去的才会被记为已处理。
- **立即触发一次**：编辑页「最近自动回复」右上角的 ghost 按钮，跳过 5 秒静默窗口和「已尝试过」判重，直接按当前设定回一次最后那条消息。规则没开、最后一条是自己发的、微信没连上时按钮禁用或以 Toast 说明原因，绝不静默无反应。
- **倒计时**（仅全自动回复）：草稿生成后倒计时（默认 5s）再发，期间可在记录或回复台取消，或「暂缓」转成人工确认；同一会话来了新消息则旧草稿作废，永远只回最新的那条。
- **记录**：最近 N 条（触发消息 → 回复；时间；状态 已发送 / 待确认 / 即将发送 / 失败 / 已忽略），待确认的记录直接在行内确认发送；hover 可查看原消息（定位到触发的那条真实消息）/ 撤回（2 分钟内）；「查看全部」在右侧抽屉展示可筛选的完整列表，抽屉里同样可以确认或忽略。
- **口吻**：AI 生成的回复以主人本人身份写，参考该会话里主人最近发过的原话（长度、标点、表情、是否连发）；聊天记录以逐行对话的形式给模型；发出前去掉引号包裹、Markdown 和主人从不打的句号。不承认是 AI，不写客服腔，不确定的事用本人口气含糊带过。
- **Agent 起草**：Agent 面板里让 Agent「帮我回一下」时，它调用 draft_reply 把草稿放进回复台等你确认，只有你明确说「直接发」才会走需要审批的 send_message。

---

## 4. AI 克隆

- **状态机**：`未克隆 → 克隆中 → 已克隆`，另有 `失败`（可重试）。状态存在联系人上，左列第二行随之变化（未克隆 · 消息数 / 克隆中 · 进度 / 已克隆 · 版本 · 样本数）。
- **未克隆**：Workspace 显示居中的确认页 = 大头像 + 标题 + 说明 + 数据统计 chip + 隐私提示 + 参数（训练范围、模型）+ 主按钮「开始克隆」+ 次按钮「先看看样本对话」（弹窗展示随机样本）。消息数低于阈值（建议 300）给警告但允许继续。
- **克隆中**：同一 Tab 内换成进度卡（头像 + 已用 / 预计时间 + 进度条 + 步骤列表 + 取消）；可切走 Tab，进度在左列可见；完成后 Toast 并自动切到已克隆页；失败弹窗区分「模型错误（换本地模型 / 重试）」与「有效消息过少（扩大范围）」。
- **已克隆**：头部（分身名 + 版本徽标 + 元信息 + 重新克隆 / 删除），下方左右两栏：
  - 左 = 与分身试聊（普通聊天流 + 输入框；分身消息带「分身」徽标，hover 可 👍 / 👎 / 「不像」→ 纠正 popover，纠正内容可保存为样本并更新口头禅）。发送失败不保留未送达的气泡，行内「重试」重发上一条文本；「保存为样本」成功或失败都只弹一次 Toast。
  - 右 = 人格画像（语气标签 / 口头禅 / 回复习惯 KV / 样本消息），全部可手动修正（标签增删、KV 编辑、样本替换删除），底部可重新提炼。
  - 重新克隆会覆盖画像，弹窗里可勾选保留手动修正；删除克隆为危险确认并说明对自动回复规则的影响。
- 联系人列表只包含单聊；顶部分段「已克隆」带计数；右键菜单按状态给不同动作。「开始克隆… / 重试克隆…」只打开克隆 Tab（确认页含隐私说明、训练范围与模型），不直接开始；「取消克隆」与进度卡使用同一个确认弹窗。

---

## 5. Onboarding 向导

- 四步：欢迎 → 连接微信（路径 + 账号）→ 解锁数据（密钥）→ 完成。稿子里第 2、3 步合并在一页（`检测WC_缓存目录`）。
- **欢迎页**：居中单列（logo → 标题 → 三张能力卡 → 协议卡 → 主按钮 → 步骤条）。主按钮在协议未勾选时禁用。
- **连接与解锁页**：两段卡片；每个字段 = 标签行（图标 + 名称 + ? tooltip + 一句提示 + 状态）+ 输入行（输入框 + 动作按钮）。
  - 微信数据库路径：自动获取（检测微信进程 → 读取储存位置）或浏览（系统目录选择器）；成功后标「已从缓存加载 / 已自动获取」，失败给行内错误并保留手动选择。
  - 缓存目录：可选，可恢复默认，磁盘空间不足警告。
  - 微信账号：下拉列出该目录下检测到的 wxid（带验证状态），验证通过后显示 ☑；未验证给警告并阻断下一步。
  - 密钥卡：头部「自动获取」一次拿三个密钥，卡内以步骤列表显示进度；可部分成功（缺失项标红 + 单独重试 / 内存扫描）；需要系统权限时弹指引弹窗；手动粘贴做格式校验。
  - **门禁**：「下一步」= 账号已验证 且 解密密钥已获取；禁用时 hover 给 tooltip 说明缺什么；点击后先测试连接（loading），成功 Toast 进主界面，失败弹窗指引。
  - 退出向导需确认，已填内容本地保留。
  - 完成向导时保存失败：留在向导，弹 error Toast「无法完成设置」+ 详情 +「重试」，保存成功后 Toast 自动关闭。
- **语言**：向导顶栏左侧放小号分段控件 `简体中文 | English`（与「设置 › 常规 › 语言」写同一个 `general.language`），因为完成向导前进不了设置页。

---

## 6. 通用交互规则（触发 → 形式）

| 需要做的事 | 用什么 | 去哪看样式 |
|---|---|---|
| 二级 / 不常用动作 | hover 露出 `···` → 菜单；右键同一菜单。行内已有常驻控件（toggle / 未读徽标）时，`···` 放在它**左侧**并常驻占位（hover 才可见），不得覆盖在它上面 | 各功能看板 |
| 从若干项选一个 | 下拉（带勾 + 一句说明，可带搜索） | 通用组件看板 §⑤ |
| 开 / 关 | toggle，即时生效 | 通用组件看板 §② |
| 二选一 / 三选一 | 分段控件 | 通用组件看板 §③ |
| 轻量确认 / 小表单 | popover（挂在触发控件旁） | 各功能看板 |
| Agent 工具的二次确认 | 消息流里的**内联确认卡**（面板底 + 强调色 1px 描边 + r8，⏎ 允许 / ⇧⏎ 总是允许 / Esc 拒绝），不用浮层 | Agent 面板 |
| 破坏性 / 复杂表单 / 长任务 | 对话框（危险 / 表单 / 进度三种）。确认按钮在 `onConfirm` 的 Promise 结束前自锁并转 spinner，连点只执行一次；处理函数抛错或 reject 时弹窗保持打开、按钮恢复可点，并弹 error Toast「操作失败」+ 错误详情 | 通用组件看板 §⑤ |
| 操作结果 | Toast（右下，4s，有操作不自动消失） | 通用组件看板 §④ |
| 打开链接 / 系统设置 | `app:openUrl`（http/https/mailto/系统设置 deep link）；本机文件才用 `app:openPath`，两者失败都要 Toast | — |
| 表单校验 | 控件下行内提示 + 主按钮禁用 + tooltip | 通用组件看板 §③ |
| 无数据 / 加载 / 出错 / 无结果 | 统一空态模板 | 通用组件看板 §⑥ |
| 页面渲染崩溃 | 每个 Tab、对象列表主体与 Agent 面板各自的错误边界：出错态「无法显示此内容 / 请重试，仍然失败时重启应用」+「重试」重新挂载；只影响出错的那一块 | 通用组件看板 §⑥ |
| 长列表详情 | 右侧抽屉 | 通用组件看板 §⑤ |

同一动作有多个入口时共用一个确认弹窗。例如日记的「重新生成」（顶栏按钮、日期右键、重新生成今天）都走同一个确认框，标题带上日期。

快捷键：⌘1/2/3 切功能，⌘, 设置，⌘K 搜会话，⌘F 搜索当前页，⌘N 新 Agent 会话，⌘W 关 Tab，⌘⇧T 恢复 Tab，⌘⇧A 引用到 Agent，⌘⇧B 收起 / 展开侧栏，⌘⇧J 收起 / 展开 Agent 面板，⌘⇧L 工作台 ↔ Agent 窗口（Agent 模式下的含义见「Agent 窗口」一节）。

---

## 7. 数据与配置边界

- 所有微信数据（会话、消息、联系人、媒体）来自本地解密后的数据库与索引；界面不直接读微信文件。
- 密钥、路径、账号、模型配置、规则、克隆画像、记忆文件都是**本机持久化**的配置，设置页与向导只是它们的编辑入口。
- 发往第三方模型的内容只在用户配置了在线模型时发生，并且界面上对应位置要有说明（见 `CLAUDE.md` §6）。
- 统计数字（消息数、未读、今日回复次数、上下文 token）都是实时计算值。

---

## 8. 视觉真源索引（Figma node id）

| 页面 / 状态 | node id |
|---|---|
| 欢迎页 | `91:417` |
| 检测WC_缓存目录（连接 + 解锁） | `135:415` |
| Main（聊天预览） | `115:415` |
| Settings 常规 / 账号 / AI 接入(本地) / AI 接入(在线) / 记忆 / 版本 | `125:415` / `128:462` / `128:926` / `142:415` / `128:1390` / `128:1854` |
| AI 接入 · 模型厂商 / 接入弹窗 / 推理强度浮层（参考稿，另一产品的浅色风格，只取布局与图标） | `164:271`（section）/ `164:4211` / `164:4892` · `164:5500` / `164:3520` · `164:3532` |
| 自动回复 | `143:415` |
| AI 克隆 未克隆 / 克隆中 / 已克隆 | `144:415` / `156:1528` / `145:415` |
| 在页面中的浮层示例：权限下拉 / 会话右键 | `156:415` / `156:964` |
| 交互态看板：Main 列表·Tab·消息 / Main Agent / Settings / 自动回复 / AI 克隆 / 检测WC / 通用组件 | `149:415` / `150:415` / `151:415` / `152:415` / `153:415` / `155:415` / `154:415` |

链接格式：`https://www.figma.com/design/l1f9ojImxG3cosmDbtUZT2/AIWC?node-id=<id 冒号换短横线>`。看板里每个组件的小标题就是它的触发位置。


## 外观与操作层级更新（2026-09-10）

本节覆盖旧版固定品牌色及机械限制单个主按钮的约定。主题选择使用系统 / 浅色 / 深色三张可点击预览卡。浅色、深色独立保存强调色、背景、前景、字体和 0–100 对比度；编辑非当前模式时仅更新对应预览，实际模式切换后应用。Codex 默认基线：浅色 `accent #4E82EF / background #FFFFFF / surface #E9E9E9 / font #000000 / contrast 35`，深色 `accent #4E82EF / background #181818 / surface #393939 / font #FFFFFF / contrast 81`。背景控制内容底色，前景控制顶部栏、侧栏、列表面板并派生卡片与浮层，字体控制正文并派生次级文字。支持恢复单个模式默认配色。所有预览与实际界面共享配色派生逻辑；旧配置首次加载时补齐前景色，用户已有颜色保持不变。

按钮统一复用 kit/Button，尺寸使用 default / sm，页面不得自行覆盖颜色、圆角或高度。每个独立任务区的主要操作使用 primary（包括数据库密钥与图片密钥的自动获取）；次要操作使用 outline，轻量辅助操作使用 ghost，行内链接使用 link，破坏性确认使用 danger。同级操作保持相同层级。强调色只来自当前模式 token；状态色及消息内容色保留其语义。每个对象列表都有受限高度的独立滚动区域。

取色器：左侧圆形色样打开饱和度/明度面板与色相滑轨，右侧直接编辑 HEX；控件背景展示颜色本身。面板提供可用键盘操作的饱和度/明度滑轨。拖动实时显示控件局部颜色，结束后才应用全局配色并持久化；文本回车或失焦提交，错误输入显示行内提示。对比度滑块沿用手势结束提交，避免每帧保存配置。

## 透明效果、截断与文案密度（2026-09-13）

**透明效果**：设置 › 常规 › 外观 新增开关，新用户默认开启，即时生效并持久化到 `general.transparency`；「恢复默认外观」会恢复当前模式的配色并重新打开它，用户已保存的开关状态不被覆盖。系统不支持时开关显示为关闭并置灰。开启后只有 IconRail 与对象列表变成毛玻璃，工作区与 Agent 面板保持不透明，保证正文对比度。原生层由主进程提供：macOS 为 `sidebar` vibrancy，失焦时保持激活；Windows 11 22H2 及以上为 acrylic；其他系统不支持，开关置灰并在 tooltip 里说明。渲染层色层全部由当前配色 token 派生：功能栏为前景色 18%，列表为面板色 12%，列表内搜索框与工具按钮为内容色 28%。另有 1px 顶部高光（字体色 14%）和从顶部 36% 处淡出的光泽（字体色 6%）。原生材质在白色网页与深色编辑器上方都能保持文字可读，因此色层保持浅；2026-09-14 在 macOS 26 真实窗口里对比，旧值 52% / 46% 下玻璃接近不透明灰色，新值下壁纸色彩饱和度提升约 35%–55%。以上只在 `<html data-glass="true">` 下生效，自定义配色与浅色主题会自动得到对应的玻璃色。

**截断**：所有单行文字溢出以「…」结尾。徽标最宽 160px，Toast 操作最宽 120px，工具提示最宽 280px，说明型工具提示最宽 240px 且可换行。输入框的值与占位符同样以省略号结尾。对象列表头：「更多筛选」移到搜索框右侧，与刷新按钮同为 32px 方形工具；筛选 chip 不收缩，计数超过 999 显示「999+」；「清除筛选」改为 chip 行末尾的图标按钮，列表窄于 272px 时隐藏，此时可用筛选菜单里的同名项或点「全部」清除。

**问号说明**：kit `HelpTip` 为 13px 问号图标，16px 点击区，弱色，hover 变次级色，可用 Tab 聚焦。它挂在设置行标题、分区标题与向导字段标签之后，无障碍名称为「{主题}说明」。

**窄设置页**：设置行宽度小于 380px 时控件移到文字下方。设置 Tab 宽度小于 720px 时内容区左右内边距降为 16px，小于 640px 时左侧导航收窄到 168px。


## AI 宠物（2026-09-13）

与 Codex Pets 同一格式、同一套状态语义的 Agent 伙伴。宠物只**反映** Agent 状态，不说话、不生成内容（CLAUDE.md §5：Agent 只在右侧面板说话）。

**格式**：宠物包 = `pet.json` + `spritesheet.webp/png`，8 列、每帧 192×208；v1 为 9 行（1536×1872），v2 在末尾多两行「左看 / 右看」（1536×2288，`spriteVersionNumber: 2`）。行序：待机、右跑、左跑、挥手、跳、失败、等待、奔跑、完成（review）。帧时长沿用 Codex：待机 6.6s 一轮的慢呼吸，其余状态每帧 120–150ms、末帧加长。

**在哪出现**
- Agent 面板：站在输入框卡片上沿右侧（压住卡片边 6px），消息列表底部留出宠物高度，最新一条消息永远不被遮住；滚动查看历史时文字可从宠物身后经过。
- Agent 面板收起时：缩小到 40px 侧边条里 Agent 按钮下方，继续跟随状态，点击展开面板，没有气泡。
- 设置 › 通用 › 宠物：当前宠物（预览 + 开关）→ 我的宠物 → 宠物图库。

**状态（只看当前激活的 Agent 会话）**，优先级从高到低：
| 条件 | 动作 | 气泡 |
|---|---|---|
| 有待二次确认的工具调用 | 等待（循环） | 需要你确认 · 工具摘要（常驻，点一下收起） |
| 正在生成 | 奔跑（循环） | 仅当有工具在运行时：正在处理 · 工具摘要 |
| 本轮刚结束且成功 | 完成动作播 3 轮后回待机 | 完成了 · 回答预览，8 秒 |
| 本轮出错 / 步数上限 / 重复调用 / 超时 | 失败动作播 3 轮后回待机 | 出错了 · 错误信息，15 秒 |
| 用户主动停止 | 无反应 | 无 |
| 空闲 | 待机；每 12–26 秒随机挥手 / 跳 / 左右看（v2） | 无 |
- 「刚结束」只来自本次运行中观察到的 生成中 → 结束 的跳变，打开旧会话不会补播。
- 点击宠物：跳一下；悬停：挥手（5 秒内不重复）；右键：摸摸它 / 更换宠物… / 隐藏宠物。忙碌（生成 / 等确认）时不响应点击动作。
- 气泡样式与 tooltip 同一套（raised 底、r6、toast 阴影），不新增浮层样式。
- 「宠物动画」关闭或系统「减少动态效果」开启时保持每个状态的第一帧静止姿势，姿势仍随状态变化。

**配置**（`pet`，即时生效）：`enabled`（默认开）、`current`（未设或已删除时回落到内置宠物）、`bubbles`、`motion`、`idleFlair`、`size`（小 / 中 / 大 = 缩放 0.26 / 0.34 / 0.44）。

**我的宠物**：点击即选用；悬停露出 `···`，与右键同一菜单：选用 → 在 codex-pets.net 查看 → 删除（危险确认；内置宠物不可删，删除当前宠物后回落到内置宠物）。「导入压缩包」走系统文件选择框，接受 codex-pets.net 下载的 zip（任意目录层级的 `pet.json` + 精灵图），导入后立即选用。

**宠物图库**：数据来自 codex-pets.net 社区（搜索、排序：最新 / 最受喜欢 / 浏览最多 / 讨论最多 / 随便看看、分页 30 只）。卡片显示海报帧，悬停播放挥手预览；按钮「领养 / 选用 / 正在使用」。领养 = 进度弹窗（下载宠物包 → 校验精灵图 → 保存到本机，只有取消），成功后立即选用并 Toast。四态：加载骨架、错误（重试 + 在浏览器打开）、无结果（清除搜索）、空。隐私说明常驻：浏览只加载预览图，领养只下载宠物包，不上传任何数据。

**数据与安全**：宠物保存在 `<dataRoot>/pets/<id>/`（内置宠物从 `resources/pets` 复制过来），渲染层只拿到 `aiwc-media://` 地址。主进程在写盘前按文件头校验图片尺寸与 `spriteVersionNumber` 是否一致；id 只允许 slug；图库资源只接受 https 的 codex-pets.net 地址；包大小上限 32 MB。

## 办公平台连接器（2026-09-13）

Agent 把整理好的表格推到飞书多维表格 / 钉钉 AI 表格 / 企业微信智能表格。没有独立页面：连接、授权、确认、结果都出现在 Agent 面板里对应的工具调用下方；设置 › 账号 › 办公平台负责查看状态和撤销。

**授权卡**（`office_connect` 调用行下方，与设置里的连接弹窗是同一个组件）
- 步骤列表沿用 ✓ / ⟳ / ○ / !，行高 22；右侧弱色等宽细节只放短信息（CLI 版本、账号名），不放应用 ID 这类内部值，不挤压步骤名。
- 等待浏览器时出现链接框：强调色 6% 底 + 30% 描边、r8、p10。左侧 96px 二维码（白底 r6，任何主题下都深色码白底，才能扫）+ 下方弱色「也可以用 X 手机端扫码」；右侧一句说明（已在浏览器打开…确认后自动继续）、链接（等宽强调色、单行截断、点即打开）、「打开链接」primary + 复制 IconButton、授权码（等宽）、有效期（到期变警告色）。浏览器没打开时在说明下加一行警告色提示。
- 进行中只有「取消」；完成后收成 ✓ 步骤 + 成功行「已连接飞书 · 账号」；失败给错误原文、可复制的安装命令和「重新连接」（outline）。
- 企业微信的二维码用 CLI 自己输出的绑定码（页面链接里的二维码扫不了）；钉钉优先使用已填入授权码的链接。

**确认卡**（高危确认的主体，替代原始 JSON）
- `office_push_table`：弱色「N 行 · M 列」（追加时加强调色「追加到已有表格」+ 目标链接），下方表格预览：表头 11 Medium 吸顶、每行 11、首列主色其余次色，最多 5 行 8 列，单元格 160px 截断，超出写「另有 N 行未显示，确认后一并推送」。说明句写清数据去哪（你授权的飞书账号、你的云空间）。推送不可「总是允许」。
- `shell`：等宽命令行（多行保留）+ 弱色 cwd；`escalate` 时加一行警告色「沙箱外运行：原因」；分级器给出的说明句放在下方。调用行下方的结果卡：命令、stdout / stderr（等宽、可滚）、退出码徽标、超时 / 截断 / 沙箱外标记、hint。
- `office_connect`：将经历的步骤编号列表。

**结果卡**（`office_push_table` 成功后）：面板底 r8，左侧 28px 强调色表格图标，标题可换行、下方一行「已推送 N 行到飞书多维表格」，再下一行「在飞书中打开」（outline）+ 复制链接。只打开该平台自己域名的 https 链接。

**设置 › 账号 › 办公平台**：Section（带 ? 说明 + 刷新 IconButton）→ rows 卡片三行，标题右侧状态徽标（已连接 ok / 授权过期 warn / 未连接、未安装 neutral），说明「账号 · 表格产品 · 官方 CLI」，右侧宽屏显示 CLI 版本 + 按钮（未连接：连接 outline；已连接：重新授权、断开 ghost；企业微信 CLI 不支持退出，断开禁用并以 tooltip 说明）。卡片下一句常驻数据去向说明。「最近推送」卡片列出最近 5 次推送（标题、平台 · 行数 · 时间、打开）。断开走危险确认。连接走长任务弹窗（lg、不响应遮罩、进行中不可 Esc 关闭）。

## 定时任务（2026-09-13）

参考 Figma 184-354（进门 BRM 的定时任务），改成 AIWC 的四列：rail 第四个功能（青绿渐变 tile，⌘4）→ 左栏任务列表 → 工作区编辑器 Tab；不做卡片网格、不做弹窗式新建。

**左栏**：顶部一条 outline「＋ 新建定时任务」（h30、占满宽）；段控 全部 / 已启用 / 已暂停 带计数；列表项 = 36 r8 图标格（启用 = 强调色 15% 底 + 强调色日历时钟图标；暂停 = 弱色；运行中 = Spinner）｜名称 13 Medium｜第二行「计划 · 状态」（运行中强调色、上次失败危险色）｜右侧 Toggle；hover `···` 与右键同一菜单：编辑 → 立即执行 / 停止 → 暂停 / 启用 → 分割 → 打开最近一次对话 → 分割 → 删除（红）。空态：还没有定时任务；筛选无结果给「查看全部」。

**工作区空态**（该功能自带）：日历时钟图标 + 标题 + 一句「把一段提示词交给 Agent，按时自动运行」+ primary「新建定时任务」；下方「从模板开始」两列卡片（名称 13 Medium + 计划徽标 + 说明 12 弱色，hover 抬底），点击打开预填的编辑器。

**编辑器 Tab**（kind `task`，objectId = 任务 id 或 `new`）：header h60 = 图标格 + 名称 + 徽标（已启用 ok / 已暂停 neutral / 运行中 accent / 未保存 neutral）+ 「计划 · 下次运行」+ 右侧 启用 Toggle + `···`。正文 max-w 880，分组标题 12 弱色：
- 任务：名称 Input；Agent 指令 = 和 Agent 面板输入框同构的卡片（r8、line-8 描边、content 底）：Textarea（6–18 行）在上，下方工具栏左「权限模式」下拉（Ask / Bypass / Autopilot，同一个 kit `PermissionSelect`，新任务默认 Bypass）、右「模型」下拉（kit `ModelSelect`，未指定时显示默认模型自己的名字，不写「默认模型」）。
- 执行时间：段控 每天 / 每周 / 按间隔 / Cron；时间用原生 time 输入（等宽）；星期用 chip 多选（一…日）；间隔用下拉预设；Cron 等宽输入 + 6 段说明。不显示「接下来」预览。
- 最近运行：每条 = 28 状态格（✓ ok / ✕ danger / ■ 弱色 / ▶| warn / Spinner）+ 状态 13 Medium + 触发徽标 + 「N 步被跳过」warn 徽标 + 右侧弱色「时间 · 时长 · N 次工具调用」；第二行总结（12 次色，保留换行）或错误（危险色）；被跳过列表 = 等宽 chip + link「改为 Bypass / Autopilot 并重跑」（只在更高模式能放行时出现；删除类给一句「请在 Agent 面板手动完成」）；ghost sm「打开对话」「停止」。
- 底栏 h48：左「保存于 … · 有未保存的修改」；右 ghost「立即执行」（未保存时禁用并 tooltip「先保存任务」）/「停止」、ghost 取消、primary 保存 / 创建任务（校验失败禁用 + tooltip 原因）。
- 删除走 DangerDialog；放弃修改走 ConfirmDialog；新任务保存后 `task:new` Tab 换成该任务的 Tab。

**通知**：运行完成 Toast（有被跳过步骤时为 warning「已完成，N 步未授权被跳过」），失败 Toast 常驻；动作「查看」打开任务 Tab。

**模板**：每个模板自带它需要的权限模式（推飞书 = Autopilot，其余 Bypass），用模板建的任务不会卡在没人回答的确认上。

---

## Agent 窗口（2026-09-14）

同一个窗口的第二种布局（`ui.shellMode`），规则见 CLAUDE.md §12；这里只记尺寸与行为。

```
Window (shellMode = agent)
├─ WindowChrome                         同四列（macOS 红黄绿灯 / Windows 标题栏按钮）
└─ Body
   ├─ Sidebar        260（220–400，可收起为 0）  面板色
   │   ├─ 新会话 按钮（outline，右侧 ⌘N 键帽）
   │   ├─ 搜索框（⌘K）
   │   ├─ 分段：会话 | 定时任务
   │   ├─ 会话列表：分组 固定 / 今天 / 昨天 / 近 7 天 / 更早；行 = 图标 + 标题 + 第二行（引用对象 / 无引用 / 未发送）+ 右上时间；hover ···；右键同菜单
   │   │   （定时任务视图 = 对象列表 tasks 的分段 chip + 列表体，点击在工作区栏打开编辑 Tab）
   │   └─ 账号块（头像 36 + 昵称 + 「设置 ⌘,」）→ 设置；右侧 记忆与规则 → 设置 › 记忆
   ├─ Conversation   fill（≥ 440）                 面板色
   │   ├─ Header h40 壳色：[展开侧栏（仅收起时）] [图标 + 标题（点击重命名）] [引用 chip] … [新会话] [···] [工作区栏开关] [工作台 ↗]
   │   ├─ MessageList（居中，最宽 760；空态 = 建议提示）
   │   └─ Composer（居中，最宽 760；权限模式 ｜ 上下文环 + 模型 + 发送 / 停止；宠物栖在右上角）+ © 行
   └─ Workspace pane 560（400–1200，按需）           Workspace 组件本体（Tab 条 + Tab）
```

- **显示 / 隐藏工作区栏**：有 Tab 且未手动隐藏时显示；`tabsStore.openSeq` 每次 `open()` 自增，任何 `tab.open*`（含重新激活已开 Tab）都会重新显示；最后一个 Tab 关闭后隐藏并禁用开关（tooltip 说明「引用、生成的文件与设置会在这里打开」）。宽度受 `min(配置宽, 100vw − 侧栏 − 440)` 约束，对话列永不小于 440。
- **入口 / 返回**：面板 Tab 条最右「Agent 窗口 ↗」；窗口头部最右「工作台 ↗」；⌘⇧L；视图菜单「Agent 窗口 / 工作台」；设置 › 常规 › 布局（分段：工作台 | Agent 窗口，即时生效）。切换失败 Toast「切换布局失败」+ 详情并回滚。
- **进入窗口**：输入框自动聚焦；没有打开的会话时自动新建一个（与展开面板相同）。关闭最后一个会话 → 立即新建一个空会话，不回工作台。
- **快捷键**（Agent 模式）：⌘⇧B 侧栏；⌘K 侧栏搜索；⌘⇧J、⌘1–4 回工作台；其余不变。
- **右键 / ··· 菜单**（侧栏行）：打开会话（非当前）→ 重命名 → 固定 / 取消固定 ｜ 导出对话 → 压缩上下文 → 清空上下文 ｜ 关闭标签 → 关闭其他标签（仅面板已打开的会话）｜ 删除会话（红字最后）。未发送的草稿只能重命名 / 删除。头部 ··· = 面板的会话菜单。
- **状态**：侧栏行 trailing：正在生成 = spinner；等待确认 = 警告色 `!`（tooltip「等待确认」）。
- **空态**：无会话 →「还没有会话 / 点「新会话」或直接在下方输入」；搜索无结果 →「没有匹配的会话」；定时任务列表沿用其自身空态。
- 文案：`agent.window.*`、`shell.agentWindow.*`、`menu.toggleAgentWindow`、`settings.general.layout.*`、`app.layout.switchFailed`（zh / en 成对）。
