# CLAUDE.md — AIWC 界面设计原则（防漂移）

工程规范（分层、代码、测试、协作、安全红线）在 AGENTS.md，下一行把它整篇加载进来；本文件只管界面。

@AGENTS.md

> 这份文件是**约束**，不是参考。实现任何界面前先读它；每做完一个页面，回来逐条核对。
> 完整尺寸 / 文案 / 交互清单在 `DESIGN-SPEC.md`；本文只讲「为什么这样做」和「什么绝对不能改」。
> **界面支持简体中文与 English：任何文案改动都必须同时补齐两种语言，规则见 §11。**
> Figma：`https://www.figma.com/design/l1f9ojImxG3cosmDbtUZT2/AIWC`（Page 3）。有疑问以 Figma 节点为准，用 Figma MCP `get_design_context` 读，不要凭记忆猜。

---

## 1. 产品心智模型（一句话）

**AIWC = 一个本地运行的微信数据工作台，左边选功能，中间看和改，右边让 Agent 干活。**

四列，从左到右宽度可调，但默认宽度如下：

| 列 | 宽 | 角色 | 内容随什么变 |
|---|---|---|---|
| IconRail | 64 | 选**功能**：聊天 / 自动回复 / AI 克隆；底部头像 = 设置 | 不变（索引微信信息） |
| 对象列表 | 280 | 当前功能的**对象**：会话 / 联系人 | 随 rail 选中的功能变 |
| 工作区 | 自适应 | **Tab 容器**。聊天预览、设置、自动回复规则、克隆页、生成的文件，全部是 Tab | 随点击打开的 Tab 变 |
| Agent 面板 | 360 | 常驻的 AI 对话 | 不随 Tab 切换而变 |

由此推出的硬规则：
- 任何新功能/页面都**在工作区开一个 Tab**，不新开窗口、不做全屏页、不塞进侧栏。
- 设置也是 Tab（左 200 子导航 + 右内容），由左下角头像触发。
- Agent 面板永远在右边，永远是同一个组件；不要为某个功能单独做一个「AI 助手」。
- Onboarding（欢迎页 / 检测WC）是唯一的例外：单页向导，没有四列。
- **Agent 窗口**（§12）是同一个窗口的第二种布局：侧栏（会话 / 定时任务）+ 整宽对话 + 按需的工作区栏。它不是新窗口，功能与 Agent 面板完全同源，Tab 仍在（右侧的）工作区里打开。

---

## 2. 视觉语言：暗、分层、克制

### 2.1 三层背景 + 一根线，没有第四种灰
- 默认配色与 Codex 对齐：浅色背景 / 前景 / 字体为 `#FFFFFF` / `#E9E9E9` / `#000000`、对比度 35；深色为 `#181818` / `#393939` / `#FFFFFF`、对比度 81。
- 壳、面板和浮层由背景与前景色派生；次级文字和分隔线由字体、背景与对比度派生。统一经过 `paletteTokens`，禁止另设固定主题灰。
- 层与层之间只用 **1px 语义分隔线**，强度随当前主题对比度派生；**卡片不加投影**。投影只给浮层（菜单 / 弹窗 / Toast / 输入框卡）。
- 分段选中、胶囊、Toast 和浮层使用 `bg-raised` / `bg-overlay`，由当前主题派生。不要在页面里发明新的灰。

### 2.2 当前模式只有一种强调色
- 强调色来自「设置 → 常规 → 外观」，浅色、深色分别保存 accent / background / surface / foreground（字体）/ contrast，跟随系统时使用系统当前模式的配置。默认强调色与 Codex 对齐为 `#4E82EF`，组件必须引用语义 token，不得硬编码品牌色。
- 主操作使用 primary；各独立任务区的主要获取操作统一使用 primary（数据库密钥、图片密钥均为强调色）；检测 / 重试等次要操作统一使用 outline，不得为了凑“一个主按钮”把其中一个任意染色。只有明确的主次任务才用不同层级。
- 强调色统一用于主按钮、选中态（12–15% 底）、引用、进度、焦点与链接；hover / active / 透明底和按钮文字均由当前配色派生。成功、警告、危险、固定消息气泡与内容本身的颜色独立于强调色。
- **「我」发出的消息气泡是 `#132F63` 深蓝，白字**
- 语义色固定：成功 `#52c98b`、警告 `#facc15`、危险 `#f0564a`、信息 / 分身 `#5b8def` `#60a5fa`。徽标 = 色 14% 底 + 30% 描边 + 100% 字。
- 渐变只允许出现在 rail 的三个功能图标和头像上，其他地方一律纯色。

### 2.3 字：小、密、有层级
- 正文 13，说明 12，微文字 11，气泡 14/lh22，页标题 20，向导标题 22。不要用 16 以上做正文。
- 三级文字色由字体、背景与对比度统一派生为主 / 次 / 弱。说明文字永远使用弱色，永远在标题正下方。
- 中文 PingFang SC（设计稿 Noto Sans SC），数字 / 英文 / 快捷键 Inter，路径 / wxid / 密钥 / 模型 ID / Markdown 编辑器一律等宽（SF Mono）。
- 圆角固定：窗口 10、卡片 12、条目 / 输入 8、按钮 / 菜单项 6、chip 100。不要出现 4 以外的其他小圆角。

---

## 3. 同一种东西只有一种长相

- **设置行**：左「标题 13 Medium + 说明 11.5 弱色」，右控件；行间 1px 线；最后一行无线。所有设置页都是这一种行，不做表格、不做表单式左标签。
- **列表项**：头像 36 r8 ｜ 名 13 Medium ＋ 第二行 12 弱色 ｜ 右侧时间 / 徽标 / 开关；选中 = 强调色 12% 底，hover = 白 5% 底并露出 `···`。会话、联系人、自动回复规则都用它。
- **Tab**：h40，图标 14 + 文案 12.5；激活 = 与内容区同底色 + 上圆角 8 + 强调色图标 + ×；未激活 = 弱色。工作区和 Agent 面板的 Tab 是同一个组件。
- **卡片**：面板色 + 1px 白 6% + r12，内 pad 16，不加投影。
- **按钮**：只有 primary / ghost / outline / danger / link 五种，h30 r6。每个独立任务区最多一个 primary；同级操作使用相同层级，按任务主次选择 primary 或 outline。danger 只出现在破坏性确认里。
- **浮层**：菜单、下拉、popover、弹窗、抽屉、Toast、tooltip 各只有一套样式（见 SPEC §9），项高 30、带说明 42、hover 白 7%、danger 项红字。
- 需要新组件时，先问「kit 里有没有能改一个参数就复用的」；没有再新建，并同步补进 `DESIGN-SPEC.md` §9。

---

## 4. 交互约定

1. **次要操作靠 hover 露出**：列表项的 `···`、Tab 的 ×、微信消息的悬浮工具条、记录行的按钮，平时不显示。Agent 对话流例外：用户消息操作与每轮最后答复的操作栏常显，工具展开箭头常显，不给中间回复重复放操作栏。
2. **列表项 / Tab / 消息都有右键菜单**，菜单项顺序：常规操作 → 分割 → 跨功能操作（设置自动回复 / 克隆 / 引用到 Agent）→ 分割 → 危险操作（红字，永远最后）。
3. **选一个值用下拉**（带勾 + 一句说明），**改一个开关用 toggle**，**二选一 / 三选一用分段控件**。不要用 radio 做主界面里的选择。
4. **破坏性和对外动作必须二次确认**：删除记录 / 规则 / 克隆、清空记忆、撤回、推送 / 发送。弹窗默认焦点在「取消」，点遮罩不关闭。
5. **长任务 = 弹窗 + 步骤列表**（✓ 已完成 / ⟳ 进行中 / ○ 待办 / ! 失败），只保留「取消」。密钥获取、克隆、下载都是这个形态。
6. **结果用 Toast，校验用行内提示**：Toast 右下角，4 秒消失，带操作时不消失；表单错误跟在控件下面，红字 + 红描边，同时禁用主按钮并给 tooltip 说明原因。
7. **每个列表 / 区域都要有空态、加载态、错误态、无结果态**，用 SPEC §9.5 的同一个模板。
8. **快捷键**：⌘1/2/3 切功能，⌘, 设置，⌘K 搜会话，⌘F 搜设置 / 聊天记录，⌘N 新会话，⌘W 关 Tab。菜单里显示快捷键。

---

## 5. Agent 面板的规矩

- 三种权限模式：**Ask（默认）**、Bypass、Autopilot，下拉里每个模式带一句说明。Ask 下所有写入 / 推送 / 发送类工具调用先弹确认 popover（拒绝 / 总是允许 / 允许一次）；Bypass 放行写入；Autopilot 连对外发送（微信消息、推送表格）也放行；删除类操作任何模式都确认。
- 工具调用逐行展示：状态图标 + 一句话 + 耗时；点开看输入 / 输出。不要把工具调用藏进折叠的「思考中」。
- 上下文用 `@` 引用（会话 / 文件 / 联系人 / 记忆），显示为强调色 chip；`/` 触发技能。
- 上下文环常驻在输入框工具栏，hover 看明细，可一键压缩。
- 生成中必须能停；失败必须给「去改 Key / 重试」这类可执行按钮，不要只报错。
- Agent 只在右侧面板说话；工作区里的「引用到 Agent」是把内容送过去，不是在工作区里生成对话。

---

## 6. 本地优先，处处说清楚

- 凡是读取微信数据、密钥、发送到模型服务商的地方，界面上要**用一句话说明数据去哪**（本地不上传 / 会发给你配置的服务商 / 本地模型不出本机）。设置里的「本地」徽标、克隆页的隐私提示条、向导底部的强调色提示条都是这个目的，不要删。
- 密钥默认打码，👁 切明文；路径 / wxid 等宽显示并尾部截断；复制后 Toast 确认。
- 语音转文字、模型、克隆都提供**本地选项**，在线选项的表单和 Agent 模型表单完全同构。

---

## 7. 绝对不要做的事

- 不要把功能做成新窗口、抽屉或全屏页；一律工作区 Tab，入口则在左上角，同会话、克隆，自动回复这些。Agent 窗口（§12）是唯一的整窗布局，它自己也用工作区 Tab 承载引用、文件与设置。
- 不要给卡片加投影、不要加渐变背景、不要加第二种强调色。
- 不要用 emoji 当图标；图标只用 lucide 线性图标，stroke 1.75。
- 不要自造新的灰色、新的圆角、新的按钮样式、新的弹窗样式。
- 不要省略空态 / 加载 / 错误 / 禁用态。
- 不要把figma里的演示数据/假数据（Fable 5、wxid_a8k9…、1,284 条）写死进代码。
- 不要在 `src/`、`electron/` 里写死任何界面文案（中文或英文都不行），不要只补一种语言（§11）。

---

## 8. 每页完成前的自检

1. 四列宽度和背景三层是否正确？Tab 是否在工作区里打开？
2. 标题 / 说明 / 弱文字三级是否分明？路径密钥是否等宽 + 截断？
3. 所有可点的地方是否有 hover 态？列表项 / Tab / 消息是否有右键菜单？
4. 破坏性操作是否有确认弹窗？长任务是否有步骤进度？结果是否有 Toast？
5. 用 Figma MCP 截同名节点截图，和实现并排对比一次。
6. 本页所有文案是否 zh-CN / en-US 成对存在？切到 English 后是否无中文残留、无截断变形？（§11.5）
7. 改的是 Agent 面板或 Agent 相关能力？切到 Agent 窗口（⌘⇧L）再走一遍 §12.4。

---

# 当页面布局与功能实际可用性冲突时，以功能实际可用性优先

---

## 9. 工程结构（补充）

代码分层、依赖规则、内核循环、工具与审批矩阵、通道与记忆约定见 `docs/ARCHITECTURE.md`。改任何一层前先读它；新增工具 / 页面 / 通道适配器按它的「如何扩展」小节做。

## 10. 外观与滚动回归规范

- 外观配置必须经过 protocol schema 校验、持久化，并在切换模式、跟随系统、启动恢复及保存失败回滚时完整应用。
- 背景 / 前景派生面板层次，字体 / 背景及对比度派生文字层级和分隔线；自定义预览必须使用与正式界面相同的 token。显示正文对比度供用户判断可读性。
- 检查同类操作的 variant 一致性，不按按钮在页面上的位置随意分配主色。固定语义色上的文字不可引用随强调色变化的文字 token。
- 对象列表必须约束高度并拥有独立纵向滚动；列表头固定，条目不可因 flex 收缩而压扁。验证自动回复、AI 克隆和聊天在长列表与小窗口下均可滚动。

- 取色控件采用左侧色样入口 + 右侧可编辑 HEX；支持 3/6 位色号，非法输入不写配置。色板拖动仅更新控件局部预览（指针事件按 animation frame 合并），松手提交；文本回车/失焦提交，对比度同样在手势结束提交。禁止在连续 input/pointermove 中执行 config:set、磁盘写入或全局主题刷新。
- 「透明效果」（`general.transparency`，新用户默认开，「恢复默认外观」会重新打开，已保存的选择不覆盖）只作用于 IconRail 与对象列表：主进程给窗口挂原生材质（macOS `vibrancy: sidebar`，Windows 11 22H2+ `acrylic`，逻辑在 `electron/main/windows/transparency.ts`），渲染层在 `<html data-glass>` 下把两列画成由 token 派生的浅色层（功能栏 18%、列表 12%）+ 1px 顶部高光 + 极淡竖向光泽。原生材质本身保证可读性，色层调深会让玻璃看起来像不透明灰色，调整前先在真实窗口里对比。这是 §2.2「渐变只在 rail 图标与头像」唯一的例外，仅在 `data-glass` 下生效，不得扩散到工作区、Agent 面板或卡片。系统不支持时开关置灰并用 tooltip 说明，`app:getInfo().transparency` 是唯一判断依据。
- 单行文字溢出必须以「…」结尾，不允许截出半个字：`truncate` 放在文字自己的元素上，禁止放在 `flex` / `inline-flex` 容器上；它与受限祖先之间的每个 flex 子项都要 `min-w-0`。徽标、胶囊、分段项、Select 触发器、输入框已在 kit 内处理；`src/kit/truncation.test.ts` 会拦住同一 class 串里同时出现 `flex` 与 `truncate`。
- 文案密度：设置行说明、空态说明、弹窗说明各一句短话，不复述标题或按钮。需要展开解释的内容放进问号图标（`SettingRow` / `Section` / `FieldLabel` 的 `help`，kit `HelpTip`），hover 或键盘聚焦显示。§6 要求的数据去向说明保留，但写成一句短话。
- 设置行在宽度不足约 380px 时把控件移到文字下方（容器查询），不再把标题挤成一列单字。

---

## 11. 多语言（i18n）：简体中文 + English，文案必须成对补齐

> **每一次改动都适用，没有例外。** 界面上出现的任何文字——按钮、标题、说明、`aria-label`、`title`、`placeholder`、tooltip、Toast、行内报错、确认弹窗、空态、菜单、主进程抛给界面的错误、导出文件里的标题——都必须在同一个改动里同时写好 `zh-CN` 和 `en-US` 两份。只补一种语言 = 没做完。

### 11.1 文案放在哪

- 唯一位置：`packages/i18n/src/locales/{zh-CN,en-US}/<命名空间>.ts`。**zh-CN 是参考目录**，它的结构就是类型；en-US 每个文件标注为 `Messages['<命名空间>']`，少一个键、多一个键、键名拼错都**编译不过**。
- 命名空间跟源码目录走，一个目录一个文件（大的拆子目录，如 `settings/{nav,general,account,ai,memory,about}.ts`）：

  | 源码 | 命名空间 |
  |---|---|
  | `src/features/<name>/**` | `<name>`（`agent` `chat` `autoreply` `replydesk` `clone` `diary` `file` `onboarding` `settings.*`） |
  | `src/kit/**` | `kit` |
  | `src/shell/**` · `src/workspace/**` | `shell` · `workspace` |
  | `src/app/**` · `src/platform/**` | `app`（日期词在 `format`） |
  | `electron/main/menu.ts` | `menu` |
  | `electron/**` 其余（toast、抛给界面的错误、原生对话框、导出文件） | `main` |
  | 用户协议 / 隐私政策 | `legal` |

- `common` 只放多个功能里**含义完全相同**的通用词（取消、保存、重试…）；拿不准就放进功能自己的命名空间。
- 键名描述含义、不描述中文：`chat.header.exportHistory`，不是 `chat.daochu`。同一个中文词在两处含义不同，就用两个键。
- 新增命名空间：在 `zh-CN/index.ts` 与 `en-US/index.ts` 里同时登记。
- 新增一种语言：`packages/protocol/src/config.ts` 的 `LanguageSchema` 加一项 → 复制一份 `en-US` 目录翻译 → `packages/i18n/src/index.ts` 的 `catalogs` 与 `LANGUAGE_NAMES` 各加一项。界面代码一行都不用改。

### 11.2 代码里怎么用

- 组件：`const t = useT()`（`@/i18n`），`t('chat.header.exportHistory')`。`t` 放进 `useMemo` / `useCallback` 依赖，切换语言后才会重算。
- 非组件（store、model、命令处理、纯函数）：`import { t } from '@/i18n'`，**在需要文字的那一刻调用**。禁止在模块顶层常量里调用 `t()`，那会冻结在导入时的语言。标签表（选项、菜单、元数据）存 `MessageKey`，渲染时再 `t()`。
- 参数：`t('chat.header.members', { n })`。英文复数用 `{n, plural, one {# member} other {# members}}`，中文直接写 `{n} 人`；枚举分支用 `{kind, select, dm {…} group {…} other {…}}`。**两种语言的占位符集合必须一致**，`packages/i18n/src/parity.test.ts` 会逐键检查。
- 句子里夹元素（加粗、链接、图标）用 `<Trans k="…" params={{ name: <b>{name}</b> }} />`。**不要把一句话拆成前后两个键再拼接**，英文语序不同。
- 主进程：`import { t } from './i18n'`（`electron/main/i18n.ts`）。语言跟随配置实时切换；原生菜单在切换时自动重建；抛给界面的错误和推送的 Toast 在主进程里就用 `t()` 生成。
- 语言名（`简体中文` / `English`）永远用各自的原文显示，不翻译。
- 语言设置在「设置 › 常规 › 语言」，Onboarding 向导顶栏也有同一个开关；即时生效并持久化到 `general.language`，首次启动跟随系统语言，之后以设置为准。

### 11.3 不属于界面文案、不翻译的东西

- 发给模型的提示词与工具 `description`（`electron/main/prompts/**`、`autoReplyGenerate.ts`、`replyStyle.ts`、`packages/*` 里的 prompt 与工具定义）、日志、微信数据本身（消息、昵称、表情名表）、测试与 mock 数据、仅开发模式的组件库画廊（`src/features/kit`）。
- `eslint` 规则 `aiwc/no-hardcoded-cjk` 会拦下 `src/`、`electron/` 里任何含中文的字符串字面量或 JSX 文本；上面这些例外已按目录排除。新的例外必须就地写原因：`// eslint-disable-next-line aiwc/no-hardcoded-cjk -- 模型提示词，不是界面文案`。
- 业务包（kernel / substrate / gateway / memory / protocol）不依赖 i18n。**新写**的、要给用户看的结果返回**稳定代码**（如 `reason: 'account_switched'`），由主进程或渲染层映射成文案。
- 包里**已有**的中文系统消息（报错、原因、进度步骤、状态）登记在 `known` 命名空间：zh-CN 值 = 包里原文（插值处写 `{name}`），en-US 是译文。主进程在两个边界按界面语言反查翻译——IPC 返回值与抛出的错误（`ipc/register.ts`）、推送事件与 Toast（`broadcast.ts`），规则集中在 `electron/main/localizePayloads.ts`；渲染层自己调用包函数得到的文字用 `localizeKnownText()`。
  - 改了包里这类中文句子 → 同步改 `known` 两种语言的同一个键；`packages/i18n/src/known.test.ts` 会列出与源码对不上的过期条目。
  - 新 IPC 通道 / 推送事件带回这类文字 → 在 `localizePayloads.ts` 里加一个 case，否则英文界面会漏出中文。
  - 聊天内容、昵称、会话摘要（来自微信自己的 summary 字段）、模型服务商的原始报错是数据，不走这张表。
- 不要把英文写死当作“国际化”：英文界面文案同样只能来自 en-US 目录。

### 11.4 英文文案风格

- 和中文一样短。按钮 1–3 个词。原生菜单用 Title Case，界面其余位置一律 sentence case（`Export chat history`）。
- 标签、按钮、Tab、菜单项不加句号；完整句子（说明、Toast、错误）结尾标点与中文原文保持一致。
- 术语固定：会话 = chat · 聊天记录 = chat history · 联系人 = contact · 群聊 = group chat · 单聊 = direct chat · 自动回复 = auto reply · AI 克隆 / 分身 = AI clone / clone · 回复台 = reply desk · 记忆 = memory · 日记 = diary · 密钥 = key · 数据库 = database · 索引 = index · 服务商 / 厂商 = provider · 模型 = model · 技能 = skill · 工具调用 = tool call · 二次确认 = approval · 权限模式 = permission mode · 上下文 = context · 压缩 = compact · 设置 = settings · 常规 = general · 账号 = account · 微信 = WeChat · Agent 保持 Agent。
- 布局必须容得下英文（通常比中文长 1.5–2 倍）：不给文字写死宽度；按钮、Tab、分段控件、徽标、列表副标题用 `truncate` 并给 `title`；尺寸规格（§2、§3）不因语言改变。

### 11.5 每次改动的收尾检查（与 §8 一起做）

1. 新增 / 修改的每一处可见文字，zh-CN 与 en-US 各有一条，且在**同一个改动**里；改中文时同步改英文的同一个键；删功能时两边的键一起删。
2. `npm run typecheck`：缺键、多键、拼错键为零。
3. `npx vitest run packages/i18n`：两种语言占位符一致。
4. `npx eslint src electron`：`aiwc/no-hardcoded-cjk` 为零。
5. 在「设置 › 常规 › 语言」切到 English 看一遍改过的界面：没有中文残留、没有被截断到看不懂、没有撑破布局；再切回中文确认无回归。
6. 测试默认跑在 zh-CN，现有的中文断言保持有效；需要断言英文时在测试里 `setLanguage('en-US')`，并在 `afterEach` 里调用 `__resetLanguageForTests()`。

---

## 12. Agent 窗口（Agent 模式）：与 Agent 面板功能同源，只是布局不同

> 2026-09-14 新增。给「只想和 Agent 说话」的用户一个整窗界面（参考 Cursor 的 Agents Window）。**它不是新窗口、不是阉割版**：Agent 面板能做的每一件事，Agent 窗口都必须能做；反过来也一样。

### 12.1 心智模型

- `ui.shellMode = 'workbench' | 'agent'`，持久化，启动时恢复。同一个 BrowserWindow、同一套 zustand store；`App` 只是在 `<Shell/>` 与 `<AgentWindow/>` 之间二选一挂载。
- 布局：`[侧栏 260（可收起 / 可拖 220–400）] | [对话列 fill，消息与输入框居中最宽 760] | [工作区栏 560（按需，可拖 400–1200，对话列至少保留 440）]`。
- 工作区栏就是四列里的 `Workspace`（Tab 容器）本身：Agent 模式下常驻挂载但默认隐藏，有 Tab 打开就显示，最后一个 Tab 关闭就隐藏；头部按钮可手动隐藏，再打开任何 Tab（含已打开的）会重新显示。所有 `tab.open*`（@ chip、证据引用、生成的文件、设置、定时任务编辑）都在这一栏里打开，对话不被打断，也不把用户踢回工作台。
- 入口：Agent 面板 Tab 条最右侧的「Agent 窗口 ↗」胶囊。返回：Agent 窗口头部右侧的「工作台 ↗」胶囊。另有 ⌘⇧L、视图菜单「Agent 窗口 / 工作台」、设置 › 常规 › 布局。所有入口都只走 `shell.setMode` / `shell.toggleMode`（`src/app/shellMode.ts`），不要另写一条改配置的路径。

### 12.2 同源约束（防漂移的硬规则）

- 两个界面共用 `src/features/agent/useAgentSurface.ts`（状态、订阅、动作、弹窗）与 `ThreadConversation.tsx`（消息流 + 输入框 + 权限 / 上下文环 / 模型工具栏 + 宠物 + 会话弹窗）。**给 Agent 加任何能力，改这两个文件；不得只在 `AgentPanel.tsx` 或 `window/` 之一实现。**
- 会话动作菜单只在 `threadMenus.ts` 里定义：面板的 Tab 右键 / 历史行 / ··· 与窗口的侧栏行 / 头部 ··· 都由它生成。新增一个动作 = 在这里加一项，两个界面自动都有。
- 窗口侧栏行必须含：打开 / 重命名 / 固定 / 导出 / 压缩上下文 / 清空上下文 /（已在面板打开的）关闭标签 / 关闭其他标签 / 删除。头部必须含：点标题重命名、引用 chip（点开对象）、新会话、··· 会话菜单、工作区栏开关、工作台。侧栏还有：新会话（⌘N）、搜索（⌘K）、「会话 / 定时任务」切换、账号块 → 设置、记忆与规则 → 设置 › 记忆。
- 快捷键在 Agent 模式下的含义：⌘⇧B 收起 / 展开侧栏；⌘K 聚焦侧栏搜索；⌘⇧J 与 ⌘1–4 回到工作台（⌘1–4 同时选中该功能）；⌘N / ⌘W / ⌘⇧T / ⌘⇧A / ⌘, 不变；`agent.expand` 在窗口里是空操作但必须有处理者（不然命令总线报「无处理者」）。
- 关闭最后一个会话：面板收起；窗口不收起而是新建一个空会话（`agentStore.closeThread` 按 `ui.shellMode` 判断）。
- 侧栏的「定时任务」视图复用对象列表注册表里 `tasks` 的列表组件与分段（`getObjectList('tasks')`），不复制一份任务列表；任务编辑在工作区栏打开。
- 侧栏行显示运行状态：正在生成 = spinner，等待确认 = 警告色 `!`，未发送的草稿在第二行标「未发送」。

### 12.3 视觉

- 侧栏与对话列用面板色，头部用壳色（同 Tab 条），工作区栏沿用 `Workspace` 自己的壳 / 内容色；三者之间只有 1px 线，没有投影。
- 头部 h40 是拖动区（`.shell-agent-header`，控件自动 no-drag）。macOS：侧栏顶部留 44px 给红黄绿灯，侧栏收起时头部左侧留 76px。Windows：右上角给标题栏按钮留 100px（工作区栏隐藏时给头部，显示时给它的 Tab 条）。
- 透明效果只作用于侧栏（同对象列表的玻璃层）；对话列与工作区栏保持不透明。
- 布局类名 `shell-agent-*` 只由 `src/shell/AgentWindowFrame.tsx` 渲染并在 `shell.css` 定义（`shell.css.test.ts` 会拦），`src/features/agent/window/` 里不写布局 CSS。列宽与侧栏收起状态经 `src/shell/agentWindowLayout.ts` 持久化到 `ui.*`。

### 12.4 每次改 Agent 面板后的自检

1. 同一改动在 Agent 窗口里也能用（⌘⇧L 切过去看）：消息流、输入框工具栏、@ / 技能、审批卡、错误卡按钮、压缩、宠物、Toast。
2. 新的会话动作出现在侧栏行右键 / ··· 与头部 ··· 里。
3. 新的 `tab.open*` 在窗口里打开到工作区栏。
4. `src/features/agent/window/AgentWindow.test.tsx` 与 `AgentPanel.test.tsx` 都通过；文案 zh / en 成对（`agent.window.*`）。
